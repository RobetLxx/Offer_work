package LeetCode.problem;/** * @author: lingjunhao * create at:  19/4/10  上午10:17 * @description: Java的atoi函数 * 字符串转变为数字 */public class java_atoi {    public int myAtoi(String str) {        if (str == null || str.length() == 0){            return 0;        }        str = str.trim();        int n = 0;        int sign = 1;        for (int i = 0; i < str.length(); i++) {            if (str.charAt(i) == '-' && i == 0)                sign = -1;            else if (str.charAt(i) == '+' && i == 0)                sign = 1;            else if (str.charAt(i)  >= '0' && str.charAt(i) <= '9') {                int prev = 0;                for (int j = 1; j <= 10; j++) {                    prev += n;                    if (n < 0 && prev > n)                        return Integer.MIN_VALUE;                    else if (n > 0 && prev < n)                        return Integer.MAX_VALUE;                }                prev += sign *  (str.charAt(i) - '0' );                // Check for overflow again.                if (n < 0 && prev > n)                    return Integer.MIN_VALUE;                else if (n > 0 && prev < n)                    return Integer.MAX_VALUE;                n = prev;            }            else                return n;        }        return n;    }}