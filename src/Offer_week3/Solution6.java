package Offer_week3;/** * @author: lingjunhao * create at:  19/4/25  下午7:40 * @description: 顺时针打印矩阵 */public class Solution6 {    public int[] printMatrix(int[][] matrix) {        int idx=0;        int dir=0;        int []res={};        if (matrix.length==0){            res=new int[0];            return res;        }        int m=matrix.length;        int n=matrix[0].length;        boolean [][]used=new boolean[m][n];        int len=matrix.length*matrix[0].length;        res=new int[len];        int x=0,y=0;        int dx[]={0,1,0,-1};        int dy[]={1,0,-1,0};        while (idx<len){            res[idx++]=matrix[x][y];            used[x][y]=true;            int xi=x;int yi=y;            for (int i = 0; i < 4; i++) {                //一直往一个方向走                xi=x+dx[dir];                yi=y+dy[dir];                if (xi<0||yi<0||xi>=m||yi>=n||used[xi][yi]){                    dir=(dir+1)%4;//直到尽头转向                }else break;//到最中心结束循环            }            x=xi;            y=yi;        }        return res;    }}